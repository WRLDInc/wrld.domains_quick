name = "wrld-domains"
compatibility_date = "2024-11-27"
pages_build_output_dir = "dist"

# Custom domain (wrld.domains) configured via Cloudflare Pages dashboard
# This project uses simplified WHMCS integration (direct form posts)
# No API secrets needed - all auth/domain search handled by WHMCS directly
